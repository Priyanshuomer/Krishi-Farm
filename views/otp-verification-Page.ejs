<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Email</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f4f4f4;
        }
        .container {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 400px;
            width: 100%;
        }
        h4 {
            margin-bottom: 20px;
            color: #333;
        }
        .form-control {
            margin-bottom: 15px;
            padding: 10px;
        }
        .btn-custom {
            background-color: #28a745;
            color: white;
            padding: 10px;
            width: 100%;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .btn-custom:hover {
            background-color: #218838;
        }
        .resend-link, .back-link {
            display: block;
            margin-top: 10px;
            color: #007bff;
            text-decoration: none;
            font-size: 14px;
        }
        .resend-link:hover, .back-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <% if (locals.email) { %>
    <div class="container">
        <% if (locals.errorMsg) { %>
        <div class="alert alert-danger" role="alert">
            <%= errorMsg %>
        </div>
        <% } %>
        <h4>Email Verification</h4>
        <p>Enter the OTP sent to <strong><%= email %></strong></p>
        <form method="post" action="/user/generate-otp?email=<%= email %>&password=<%= password %>&full_name=<%= full_name %>&mobile=<%= mobile %>&aadhar=<%= aadhar %>">
            <input name="otp" type="password" class="form-control" placeholder="Enter OTP" required>
            <button type="submit" name="submit-btn" class="btn btn-custom">Verify Email</button>
        </form>
        <a href="/user/generate-otp?email=<%= email %>&password=<%= password %>&full_name=<%= full_name %>&mobile=<%= mobile %>&aadhar=<%= aadhar %>" class="resend-link">Resend OTP</a>
        <a href="/user/sign-up" class="back-link">Back to Sign-up Page</a>
    </div>
    <% } else { %>
    <div class="container">
        <p class="text-danger">Something went wrong. Please try again.</p>
    </div>
    <% } %>
</body>
</html>
