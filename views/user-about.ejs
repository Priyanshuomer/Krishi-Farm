<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About User</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }
        .container {
            max-width: 500px;
            margin: 50px auto;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .profile-img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #ddd;
            margin-bottom: 15px;
        }
        .user-info label {
            font-weight: bold;
            color: #333;
        }
        .user-info div {
            margin-bottom: 10px;
        }
        .btn-logout {
            margin-top: 15px;
            background-color: #dc3545;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
        }
        .btn-logout:hover {
            background-color: #b02a37;
        }
    </style>
</head>
<body>

    <% if (locals.user) { %>
        <div class="container">
            <!-- Profile Image (Placeholder if no image) -->
            <img src="https://madhyapradesh.pscnotes.com/wp-content/uploads/2020/05/shutterstock_1116673256-1.jpg" alt="Profile Image" class="profile-img">
            
            <h2 class="mb-3">User Details</h2>

            <div class="user-info">
                <div><label>Email:</label> <%= user.email %></div>
                <div><label>Full Name:</label> <%= user.full_name %></div>
                <div><label>Aadhar:</label> <%= user.aadhar %></div>
                <div><label>Mobile Number:</label> <%= user.mobile %></div>
            </div>

            <a href="/user/logout" class="btn btn-logout">Logout</a>
        </div>
    <% } else { %>
        <div class="container">
            <p>Please Login To See Your Details</p>
            <a href="/user/login" class="btn btn-primary">Login</a>
        </div>
    <% } %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
